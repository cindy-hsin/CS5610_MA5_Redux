import counterActions from './counterActions'
// import actions related to other features here, from the same 'action' folder
const allActions = {
  counterActions //,
  // otherActions
}

export default allActions;